<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.iti3">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".vabout" />
        <activity android:name=".vti" />
        <activity android:name=".vdti" />
        <activity
            android:name=".MainUpdel"
            android:label="Update dan Delete" />
        <activity
            android:name=".MainRead"
            android:label="Data Tempat Ibadah" />
        <activity
            android:name=".MainCreate"
            android:label="Tambah Data Tempat Ibadah" />
        <activity android:name=".WelcomeActivity"
            android:theme="@style/AppTheme1">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"/>
    </application>

</manifest>